query Employee($id: ID!) {
    employee(id: $id) {
        ...EmployeeDetails
    }
}

query Employees {
    employees {
        ...EmployeeDetails
    }
}


fragment EmployeeDetails on Employee {
    id
    firstName
    lastName
    age
    gender
    projectId
    project {
        id
    }
}
